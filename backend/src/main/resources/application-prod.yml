# Production profile. All sensitive values MUST come from the environment.
spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

management:
  endpoint:
    health:
      show-details: never

logging:
  level:
    com.pipeforge: INFO
    org.springframework: WARN
    org.hibernate.SQL: WARN
